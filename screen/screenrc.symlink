# remap escape key
escape ``

# Get rid of startup message
startup_message off

# Fix colors
term screen-256color

# Use a large scrollback buffer
defscrollback 5000

# Have screen read zsh config
# defshell -zsh

# status
hardstatus on
hardstatus alwayslastline "%{=b}%{G} Screen(s): %{b}%w"
